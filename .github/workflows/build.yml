name: Build Image

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  buildx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: docker/setup-buildx-action@v1
        id: buildx
        with:
          install: true

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-cache

      - name: Build jupyterlab-mlops
        uses: docker/build-push-action@v2
        with:
          context: jupyterlab-mlops
          builder: ${{ steps.buildx.outputs.name }}
          push: false
          load: true
          tags: jupyterlab-mlops:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new

      - name: Publish jupyterlab-mlops (dev)
        uses: mattes/gce-docker-push-action@v1
        with:
          creds: ${{ secrets.PUBLIC_GCR_DEPLOY_SA_KEY }}
          src: jupyterlab-mlops:latest
          dst: gcr.io/getindata-images-public/jupyterlab-mlops:dev

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
